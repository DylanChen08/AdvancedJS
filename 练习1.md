好，我们直接按真正前端面试方式来。

我不会先问定义。

而是：

看你是否真的理解：

* this
* bind
* 原型链
* new
* apply
* 闭包
* instanceof

这些是怎么串起来的。

你直接回答即可。

⸻

第一题（基础但很关键）

function test() {
  console.log(this.name);
}
const obj = {
  name: 'Dylan'
}
const fn = test.bind(obj);
fn();

输出什么？

为什么？

重点解释：

bind

到底做了什么。

⸻

第二题（真正考 this）

var name = 'window';
const obj = {
  name: 'obj',
  say() {
    console.log(this.name);
  }
}
const fn = obj.say;
fn();

输出什么？

为什么？

这里：

obj.say

赋值给：

fn

之后，

到底发生了什么？

⸻

第三题（bind 预存参数）

function add(a, b, c) {
  console.log(a, b, c);
}
const fn = add.bind(null, 1, 2);
fn(3);

输出什么？

为什么？

这里：

1 2

到底存去了哪里？

⸻

第四题（真正难度开始）

function Person(name) {
  this.name = name;
}
const obj = {
  name: 'obj'
}
const BindPerson = Person.bind(obj);
const p = new BindPerson('Dylan');
console.log(p.name);
console.log(obj.name);

输出什么？

为什么？

重点：

为什么：

new

会让 bind 失效？

或者说：

为什么：

this

最后不是 obj？

⸻

第五题（真正核心）

为什么：

this instanceof fNOP

能够判断：

是不是通过 new 调用？

这里请你解释：

* new 到底做了什么
* prototype 怎么连起来
* instanceof 为什么成立

这个是真正区分：

“背源码”

和

“真的懂 JS”

的题。

⸻

第六题（原型链）

为什么这里：

fBound.prototype = new fNOP();

不能写成：

fBound.prototype = this.prototype;

会有什么问题？

这是高级面试题。

⸻

第七题（闭包）

function test(a) {
  return function(b) {
    console.log(a + b);
  }
}
const fn = test(1);
fn(2);

为什么：

a

没有消失？

谁保存了它？

内存结构怎么理解？

⸻

你可以一题一题答。

我会像真正腾讯/字节前端面试一样：

* 抓你有没有真正理解
* 抓逻辑漏洞
* 抓是不是背答案
* 抓底层认知

你先从第一题开始。